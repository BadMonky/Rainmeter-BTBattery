# Changelog

## Unreleased

- Added `EnableState=1` option to return `-1` for offline / unavailable devices; default offline output is `0`.
- Clarified that logging is disabled by default and only enabled with `EnableLogging=1`.

- Initial GitHub release package.
- Added x64 and x86 release DLLs.
- Added example Rainmeter skin.
- Added source files for the Rainmeter C++ plugin project.
- Supports standard Windows Bluetooth battery properties.
- Adds built-in HID report profile support for DualSense-style controller battery reports.


